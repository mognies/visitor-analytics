{
  "name": "@visitor-analytics/sdk",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Web SDK for tracking visitor page history and analyzing customer interests",
  "module": "dist/analytics-sdk.js",
  "main": "dist/analytics-sdk.js",
  "type": "module",
  "scripts": {
    "build": "bun build ./index.ts --outfile ../sample-site/public/analytics-sdk.js --target browser --format esm --minify && cp _headers dist/_headers",
    "dev": "bun build ./index.ts --outfile ./dist/analytics-sdk.js --target browser --format esm --watch",
    "typecheck": "tsc --noEmit"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "analytics",
    "tracking",
    "page-views",
    "visitor-tracking",
    "visitor-analytics"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "dexie": "^4.2.1"
  }
}
